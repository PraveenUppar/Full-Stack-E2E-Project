apiVersion: apps/v1
kind: Deployment
metadata:
  name: e2e-project-frontend-deployment
  labels:
    app: e2e-project-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: e2e-project-frontend
  template:
    metadata:
      labels:
        app: e2e-project-frontend
    spec:
      containers:
        - name: e2e-project-frontend-container
          image: praveenuppar/e2e-project-frontend-image:v1
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
